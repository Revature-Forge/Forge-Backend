apiVersion: v1
kind: Service
metadata:
  name: {{ include "forge-backend.fullname" . }}
  labels:
    {{- include "forge-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forge-backend.selectorLabels" . | nindent 4 }}
